# WSSpeechManager
A Manager class to use the Speech API for iOS. It allows to detect words and phrases and execute custom blocks of code on detection. It also allows to play custom and system sounds to avoid conflicts with the AVAudioSession if it's done from outside the manager.
